#include <iostream>
//symbolic constants
#define ll long long
using namespace std;

int main(){
    // Constants: Variables that cannot be changed.
    const int n = 25;  //must be initialized
    const float PI = 3.14;
    /*
    const int n;  ----> Not allowed.
    n = 35;       ----> Left side value is not modifiable.
    */
   
    int ll x; // Data Type Modifier
   return 0;
}

// Macros: #define PI 3.14 --> Dose not occupi space in memory. Just replace the value at that variable where they are called.
// Constants: Stored in memory and fetch from memory when variable call them.